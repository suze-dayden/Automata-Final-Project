README

The main file is "file to dict.cpp". It will run on a GDB c++ compiler, like onlinegdb.
The program displays the language recognized, then asks for a single character and an expression.
The program will display the english words corresponding to the mathematical characters. The program
only recognizes single digit numbers at this time.

Other files include first attempts using simpler data structures.